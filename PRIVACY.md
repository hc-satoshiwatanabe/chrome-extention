# プライバシーポリシー / Privacy Policy

**Link Dialog for kintone**（以下「本拡張機能」）は、非公式の個人開発ツールです。本拡張機能は、いかなる情報も外部のサーバーへ送信しません。アナリティクス・トラッキング・広告関連の通信も一切行いません(本拡張機能自体が保有するサーバーもありません)。

## 保存する情報(ユーザー自身のブラウザ内のみ)

以下の情報のみを、ユーザー自身のブラウザ内のストレージに保存します。いずれも開発者を含む第三者には一切送信されません。

- 経路記録機能のON/OFF設定(真偽値1件、`storage.sync`)
- kintoneのページ遷移の経路(「経路マップ」機能用、`storage.local`)。記録されるのはページのURLのみで、直近500件を超えると古いものから自動的に削除されます。経路マップ画面の「履歴をクリア」でいつでも全消去できます
- 新しいタブでリンクを開いた際に、その経路を元のタブとつなげるための一時的な対応関係(`storage.session`。ブラウザを閉じると自動的に消え、通常は数秒〜数十秒で役目を終えます)

## 権限について

- `storage`: 上記の設定・状態を保存するためだけに使用します。
- `tabs`: 経路マップ画面でノードをクリックしてそのURLを新しいタブで開くために使用します。
- ホストパーミッション(`*.cybozu.com` / `*.kintone.com`): kintoneのページ内でのページ遷移(SPA遷移を含む)を検知して経路として記録するため、および経路マップ画面でノードのラベルを分かりやすくするためにkintoneのREST API(`app.json`でアプリ名、`app/settings.json`・`app/form/layout.json`・`app/form/fields.json`でタイトルフィールドの特定、`record.json`でそのフィールドの値、いずれもログイン中のユーザー自身のセッションを利用)を取得するために使用します。取得したレコードのフィールド値は、経路マップ画面を開いている間のみメモリ上に保持され(ブラウザのストレージには保存されません)、外部に送信されることはありません。

## 同梱しているソフトウェア

経路マップの描画に、オープンソースライブラリ[Cytoscape.js](https://js.cytoscape.org/)(MITライセンス)をファイルとして同梱しています。外部サイト(CDN等)から読み込むことはありません。詳細は[THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)をご参照ください。

## お問い合わせ

本拡張機能に関するお問い合わせは、Chrome ウェブストア/Microsoft Edge アドオンの掲載ページに記載の連絡先までお願いします。

最終更新日: 2026-09-12
