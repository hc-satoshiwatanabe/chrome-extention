# プライバシーポリシー / Privacy Policy

**Link Dialog for kintone**（以下「本拡張機能」）は、非公式の個人開発ツールです。本拡張機能は、いかなる情報も外部のサーバーへ送信しません。アナリティクス・トラッキング・広告関連の通信も一切行いません(本拡張機能自体が保有するサーバーもありません)。

## 保存する情報(ユーザー自身のブラウザ内のみ)

以下の情報のみを、ユーザー自身のブラウザ内のストレージに保存します。いずれも開発者を含む第三者には一切送信されません。

- 機能のON/OFF設定(真偽値1件、`storage.sync`)
- kintoneアプリごとに最後に選択した一覧(ビュー)のID(`storage.local`、ダイアログ内の一覧切り替えプルダウンの選択状態を次回開いたときに復元するため)
- ダイアログで開いたページ、およびダイアログ内でさらに遷移したページのURLの経路(「経路マップ」機能用、`storage.local`)。記録されるのは本拡張機能のダイアログを通じて表示されたページのURLのみで、直近500件を超えると古いものから自動的に削除されます。経路マップ画面の「履歴をクリア」でいつでも全消去できます

## 権限について

- `storage`: 上記の設定・状態を保存するためだけに使用します。
- `tabs`: ダイアログの「新しいタブで開く」を使った際に、同じURLのタブがすでに開いていればそれをアクティブにし、なければ新しいタブを開くために使用します(この判定のために開いている各タブのURLを一時的に参照しますが、保存や外部送信はしません)。また、経路マップ画面でノードをクリックしてそのURLを新しいタブで開くためにも使用します。
- ホストパーミッション(`*.cybozu.com` / `*.kintone.com`): kintoneのページ内でリンククリックを検知してダイアログ(iframe)表示に切り替えるため、ダイアログ内の一覧切り替え時にkintoneのREST API(`views.json`)からアプリの一覧構成を取得するため、経路マップ画面でノードにアプリ名を表示するためにkintoneのREST API(`app.json`)からアプリ名を取得するため、およびダイアログ内でさらに遷移した場合の経路を記録するために、ダイアログのiframe内にもスクリプトを実行します(いずれもログイン中のユーザー自身のセッションを利用)。ページの内容や取得した情報を外部に送信することはありません。

## お問い合わせ

本拡張機能に関するお問い合わせは、Chrome ウェブストア/Microsoft Edge アドオンの掲載ページに記載の連絡先までお願いします。

最終更新日: 2026-09-12
